This folder is the destination folder for all the figures generated by the code. They are not intended to be kept under revision control being binary reproducible files, hence why the folder is empty. The folder itself is instead required as the target path of the script, so it is included.
